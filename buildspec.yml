version: 0.1

phases:
  pre_build:
    commands:
      - echo Installing composer dependencies...
      - composer install
  build:
    commands:
      - composer cs
      - composer test
  post_build:
    commands:
      - composer install --no-dev

artifacts:
  files:
    - app/**/*
    - nginx/**/*
    - public/**/*
    - src/**/*
    - vendor/**/*
